version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build --workspace=@getnear/web
  artifacts:
    baseDirectory: apps/web/.next/standalone
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
